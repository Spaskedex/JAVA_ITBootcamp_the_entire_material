package IT_BootCamp_JAVA;

/*
        -- for each petlja prolazi kroz sve elemente od prvog do zadnjeg bez mogucnosti indeksiranja;
        -- i oznacava pivot koji preuzima vrednost svakog elementa u listi i moze da se koristi za stampanje (sout);
        -- i == pivot;
 */

import java.util.ArrayList;

public class Class_7_for_each_petlja {
    public static void main(String[] args) {

        ArrayList<Integer> lista = new ArrayList<>();

        for(int i=0; i<3; i++) {

        }
        for(Integer i:lista) {
                            // neki kod
            System.out.println(i);
        }
    }
}
